package service;

import block.Floor;
import unit.Enemy;
import unit.Player;

public class HitService {
    public static boolean isPlayerFloor(Player player, Floor floor) {
        return true;
    }

    public static boolean isEnemyFloor(Enemy enemy, Floor floor) {
        return true;
    }
}
